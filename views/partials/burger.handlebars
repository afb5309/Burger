<li>
	<div class="booty row col-md-12">
	{{burger_name}} 
	{{#unless devoured}}
	<div class="eat-btn">
		<button class="btn btn-danger eat-it ml-2" data-id="{{id}}">Eat it!</button>
	</div>
	<div class="eat-btn">
		<button class="btn btn-danger delete-it ml-1" data-id="{{this.id}}">Delete it!</button>
	</div>
	{{/unless}}
	{{#if devoured}}
	<div class="eat-btn">
		<button class="btn btn-danger delete-it ml-1" data-id="{{this.id}}">Delete it!</button>
	</div>
	{{/if}}
	</div>
</li>

